{
  "name": "@pablozaiden/my-cli-prettier",
  "version": "0.0.0-development",
  "description": "CLI tool to expose MCP server operations via a CLI.",
  "repository": {
    "type": "git",
    "url": "https://github.com/pablozaiden/my-cli-prettier.git"
  },
  "license": "MIT",
  "author": "Pablo Zaidenvoren",
  "keywords": [
    "ai",
    "cli",
    "mcp",
    "agents",
    "coding"
  ],
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19"
  },
  "peerDependencies": {
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.5",
    "@pablozaiden/terminatui": "0.7.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "start:interactive": "bun run src/index.ts interactive",
    "build": "bunx tsc --noEmit",
    "compile": "bun run build; export workspace_dir=$(pwd); (cd /tmp; bun $workspace_dir/src/build.ts)",
    "test": "bun test"
  },
  "config": {
    "commitHash": ""
  }
}
